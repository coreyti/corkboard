require "spec_helper"

describe Corkboard::Publishers::Pusher do
  let(:publisher) { Corkboard::Publishers::Pusher }
  let(:instance)  { publisher.new }

  describe ".new" do
    it "is pending" do
      pending
    end
  end
end
