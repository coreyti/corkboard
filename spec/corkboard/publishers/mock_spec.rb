require "spec_helper"

describe Corkboard::Publishers::Mock do
  let(:publisher) { Corkboard::Publishers::Mock }
  let(:instance)  { publisher.new }

  describe ".new" do
    it "is pending" do
      pending
    end
  end
end
