require "spec_helper"

describe Corkboard::Service::Config do
  let(:config)   { Corkboard::Service::Config }
  let(:instance) { config.new }

  describe ".new" do
    it "is pending" do
      pending
    end
  end
end
